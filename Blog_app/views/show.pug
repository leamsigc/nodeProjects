include partials/header.pug
.container
    h1.title #{post.name}
    img(src=post.image , alt="")
    .post_body
        p   !{post.body}
        strong #{post.created.toDateString()}
include partials/footer.pug