include partials/header.pug
h1  Hello from ismael Blog...
.container
    ul
        each post in blogPost
            li
                .title  #{post.name}
                img(src= post.image, alt="")
                p   #{post.body.substring(0,100)}...
                strong  #{post.created}
                a(href="/blogs/"+post._id).read  Read More
include partials/footer.pug